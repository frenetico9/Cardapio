{
  "version": 2,
  "installCommand": "corepack enable && corepack prepare yarn@1.22.22 --activate && yarn install --non-interactive --network-timeout 600000 --ignore-engines",
  "buildCommand": "yarn build",
  "outputDirectory": "build"
}
